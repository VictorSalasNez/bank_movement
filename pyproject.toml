[tool.poetry]
name = "bank-movement"
version = "0.1.0"
description = ""
authors = ["Victor Salas"]
readme = "README.md"
packages = [{include = "bank_movement"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
